public class Node {
    public int data;
    public Node next;

    public Node(int data, Node next) {
        this.data = data;
        this.next = next;
    }

    public Node(int data) {
        this(data, null);
    }

    // Q7: Complete the following method that moves the first node in the linked list
    // pointed to by intList to the end of the list and returns a pointer to the new head.
    // If the list is empty or contains only one node, do not change the list.
    // Do this without creating any new Nodes.
    public static Node frontToBack(Node intList) {

    }

    public static void main(String[] args) {

    }
}
