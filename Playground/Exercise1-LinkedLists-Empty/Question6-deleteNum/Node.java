public class Node {
    public int data;
    public Node next;

    public Node(int data, Node next) {
        this.data = data;
        this.next = next;
    }

    public Node(int data) {
        this(data, null);
    }

    // Q6: Complete the following method that removes any nodes from the list intNode
    // that contain the integer value n and returns a pointer to the new head of the list.
    // The first item might be deleted, so the head may change.
    // If all nodes are deleted, return null.
    public static Node deleteNum(int n, Node intNode) {

    }

    public static void main(String[] args) {

    }
}
